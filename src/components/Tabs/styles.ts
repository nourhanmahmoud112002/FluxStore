import { StyleSheet } from 'react-native';
import { px } from '../../common';

export const styles = StyleSheet.create({
  contentContainer: {
    flexDirection: 'row',
    gap: px(32),
  },
  container:{
    flexGrow: 0,
  }
});
