import { StyleSheet } from 'react-native';
import { px } from '../../common';

export const styles = StyleSheet.create({
  verticalContainer: {
    marginBottom: px(30),
  },
  columnWrapper: {
    gap: px(20),
  },
});
