import { StyleSheet } from 'react-native';
import { px } from '../../common';

export const styles = StyleSheet.create({
  container: {
    borderRadius: px(28),
    height: px(168),
    overflow: 'hidden',
  },
});
