import { StyleSheet } from 'react-native';
import { px } from '../../common';

export const styles = StyleSheet.create({
  container: {
    borderRadius: px(28),
    minHeight: px(141),
    overflow: 'hidden',
  },
});
